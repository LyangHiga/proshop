import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  title: {
    textAlign: "center",
  },
  formContainer: {
    marginTop: "3rem",
  },
  textField: {
    marginBottom: "1rem",
  },
  registerText: {
    marginTop: "2rem",
    textAlign: "center",
    cursor: "pointer",
  },
  registerBtn: {
    marginTop: "2rem",
  },
}));

export default useStyles;
