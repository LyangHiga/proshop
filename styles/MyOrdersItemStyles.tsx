import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  lineContainer: {
    marginTop: "1.5rem",
    cursor: "pointer",
  },
  text: {
    textAlign: "center",
  },
}));

export default useStyles;
