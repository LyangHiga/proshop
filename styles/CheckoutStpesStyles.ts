import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  container: {
    marginBottom: "2rem",
    marginTop: "-1rem",
  },
  btn: {
    textTransform: "none",
  },
}));

export default useStyles;
